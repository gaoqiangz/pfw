function msgbox(title,text)
{
	MessageBox(title,text);
}
function add(a,b)
{
	var rv= a + b;
	stdout.println(rv);
	return rv;
}
function test(btn)
{
	stdout.$n(button text:{btn["text"]});
}
function setTitle(title)
{
	host.Title = title;
	stdout.$n(new title:{host.Title});
}